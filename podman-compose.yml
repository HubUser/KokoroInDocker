version: '3'
services:
  tts-api:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./certs:/usr/src/app/certs:Z
    environment:
      - PORT=3000
      - SSL_KEY_PATH=certs/server.key
      - SSL_CERT_PATH=certs/server.cert
    devices:
      - "nvidia.com/gpu=all"
